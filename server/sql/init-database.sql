-- Einmal in pgAdmin 4 ausführen (Query Tool auf der postgres-Datenbank)
CREATE DATABASE bewerbungen;

-- Danach mit der Datenbank "bewerbungen" verbinden.
-- Tabellen legt die App beim Start automatisch an.
